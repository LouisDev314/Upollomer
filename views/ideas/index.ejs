<html>
    <head>
        <title>All Ideas —— Upollomer</title>
    </head>
    <body>
        <% ideas.forEach(idea => { %>
            <div><%= idea.title %></div>
            <div><%= idea.description %></div>
            <br>
        <% }) %>
    </body>
</html>